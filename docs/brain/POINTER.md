# This brain is HISTORICAL (migrated 2026-07-10)

The canonical trading-family brain now lives in the **private repo `tradelab`**
(github.com/skishore1676/tradelab; checkouts at `~/code/tradelab` on the dev Mac and
oldmac). It was imported from this directory at commit `3e527d6` with all ADRs and
never-re-derive facts carried verbatim (ADR-013 records the migration).

Nothing here is updated anymore: the nightly steward now runs ON oldmac inside
tradelab (`com.tradelab.brain-steward`), and Lathi Tower's C.4 source reads tradelab
locally. The dev-Mac steward (`com.mala.brain-steward`) and its scp status bridge are
retired. These files remain as the pre-migration snapshot for rollback.

Do not edit; do not cite as current. Read `tradelab/docs/brain/INDEX.md` instead.
